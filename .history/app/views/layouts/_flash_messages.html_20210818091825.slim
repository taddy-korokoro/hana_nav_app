- flash.each do |name, msg|
  #alert.alert.font-weight-normal class="alert-#{name}" role="alert"
    a href="#" class="close" data-dismiss="alert" x
    = msg

/ 一定時間でメッセージを消す設定
javascript:
  window.setTimeout(() => {
    $(".alert").fadeOut('show');
  }, 2000);
