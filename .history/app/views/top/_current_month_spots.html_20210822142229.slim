/ ----- 'top/index'で使用中 -----
.box.p-4
  .heading
   | スポット一覧
  .hr-blue
  .flower-spots
    - spots.each do |spot|
      - if spot.spot_seasons.where(season_id: current_month).each do |s|
        .card.mt-3
          = link_to spot_path(s.id), class: "flower-spot"
            .row.no-gutters
              .col-9.body
                .name.mb-1
                  = s.name
                .address
                  = spot.location
                .season.mb-2
                  = spot.time
                p
                  = spot.feature.truncate(280)
              .col-3.pl-2
                = image_tag spot.image.to_s, class: "image"
          hr
  /= paginate spots