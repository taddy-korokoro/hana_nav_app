/ ----- 'users/index, search, followings, followers' in use -----
.row.d-flex.align-items-center.px-2.py-2
  .col-10
    = render 'shared/profile_image', user: user, profile_image_confirmation: 'users_feeds_page_except'
    = link_to "#{user.name}さん", travel_records_user_path(user.id), class: 'user-detail__link--black pl-1'
  .col-2
    .d-flex.align-items-center
      .mr-3
        = render 'likes/button', travel_record: user
      i.far.fa-clipboard.fa-fw
      = user.travel_records.count
hr