= form_with model: current_user.passive_relationships.build, url: user_relationships_path(user), local: false do |f|
  = f.hidden_field :following_id, value: user.id
  = f.submit "フォローする", class: "edit-btn d-block mx-auto bg-white font-weight-bold"