.box.user-page.p-4
  .follow-list.px-3
    .row.d-flex.align-items-center
      .co.information
        | ユーザー一覧 (#{@user_count}人)
      .col.justify-content-end
        = render 'search_form'
    .hr-blue
    - if @users.present?
      - @users.each do |user|
        = render 'user_list', user: user
  = paginate @users
